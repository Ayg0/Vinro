#include "general.hpp"

int	main(int ac, char **av){
	if (ac != 2)
		return (write(2,"Error, Enter FileName\n", 22));
	/*
		open_file();
		read_file();
		get_terminal_prop();
		clear_window();
		show_it_all();
		// Functionalities:
			-> moving cursor;
			-> insert and delete;
			-> save;
	*/
	
}